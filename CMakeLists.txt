include_directories(${CMAKE_CURRENT_SOURCE_DIR})

file(GLOB IMGUI_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

file(GLOB IMGUI_INCLUDES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

add_library(ImGui STATIC ${IMGUI_INCLUDES}
        ${IMGUI_SOURCES})